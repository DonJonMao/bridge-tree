# Install the matching CANN, torch and torch_npu wheels supplied for the target
# Ascend 910B image first. BridgeTree itself has no training-only dependency.
-r requirements.txt
sentence-transformers>=3.0

