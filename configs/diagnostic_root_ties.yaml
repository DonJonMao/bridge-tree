# Opt-in sensitivity configuration, not a production scheduler or a seed sweep.
# Pre-register additional seeds as separate configurations/runs; do not select
# the best seed using evaluation answers. All resource limits remain inherited.
base_config: chain_full.yaml

dependency:
  root_tie_break: seeded_hash
  root_tie_seed: 0

runtime:
  output_dir: outputs/diagnostic-root-ties
