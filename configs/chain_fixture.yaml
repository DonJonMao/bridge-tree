# Explicit implementation validation using the same production worker.
base_config: chain_full.yaml
chain:
  initial_width: 2
  proposal_width: 2
  max_unique_memories: 12
  max_joint_contexts: 12
  max_proposal_calls: 6
  max_claim_calls: 2
  max_verify_calls: 8
runtime:
  heartbeat_seconds: 0.5
