# 同轮诊断改造：PR1–PR4 验收契约

本文件冻结本轮范围。PR1–PR4 分别提交；PR5 不实施。旧评分公式、集合序列化、选择器和默认搜索行为保持不变。根平局顺序消融默认关闭；不加入轮询、根预算保留、dense 保护、Judge、新选择器或训练。

用户最终确认：当前网络不能调用模型，本轮验收以代码、回归、真实归档离线分析与 PDF 交付为准。实际评分、重复生成和根消融由用户上传服务器后执行，不再作为本轮完成的阻断项。服务器执行仍必须满足冻结身份、预声明预算和完整归档要求；本轮交付不宣称已取得线上实验结果。

## 提交和用户工作区边界

本轮起点 HEAD 为 `39efab3dd547c522c22e3739d03e9b1904661567`。原有 tracked dirty 工作区通过只读 stash-create tree `4e346ee4d50c78fa2becec6ab1dce727388d221c` 留存，未 stash/drop/reset 用户文件。提交只纳入相对此基线的本轮改动，原有 skip-failed 修改不打包进本轮 PR。历史报告和两个 tgz 只读保留。

## 必须有直接证据的交付项

1. PR1：真实部署身份与来源等级、实际 payload/execution hash、部署隔离缓存、请求发送前审计、物理传输尝试预算、失败/拆批父子身份、token 估算与真实值分开、脱敏测试。
2. PR2：只读恢复 24/28 历史分数及四个精确缺口；每条分数保留来源；真实 bundle 并集可达、严格正边际可达；缺分 unknown；原选择器的预算感知历史回放与受限模拟分开；不能把 B∩U 当成新 bundle。
3. PR3：独立可执行 CLI、严格配置、冻结公开题目/可见记忆/评分文档/生成 ContextPlan、预列调用预算、fresh-all 评分、重复生成不吃响应缓存、trial 级续跑、不因答错重试、失败与未知结果留档、gold 仅评估阶段可见。
4. PR4：legacy_lexical 默认；seeded_hash 仅改变零优先级空 premise 初始根平局顺序；非根顺序/IDs/序列化/预算不变；跟踪搜索 trace、不同 target 成本、archive、最终集合和跨 seed 差异；不新增其他调度政策。
5. 报告：PDF 与统一诊断报告区分确定的工程/诊断改动、大机制方向、需要实验确定的具体算子；明确历史数据与当前在线结果、局部最优与全库最优、技术重复与独立问题数的边界。
6. 四个独立验收测试组、全量相关回归、命令帮助和真实归档离线执行证据；在线若缺部署身份/可达服务，记录具体未完成项，不将假服务测试当作在线实验完成。

## 科研方向与未决事项

优先方向是把相关性、条件证据贡献和停止依据拆开审视；随后根据诊断决定是否改允许回退的组合选择或跨目标预算分配。当前只记录 legacy relevance 的语义，不实现任何新 U。28 格点与重复生成用于检验 R 的排序是否与证据补入收益错位，或是否存在 archive、正路径、greedy 优化缺口。这些原因可能同时成立。

已有错误案例为事后诊断，不是新的正式测试集。所有子集最好成绩不得报告成方法准确率。新增 scorer 不得仅凭 [0,1] 或 pointwise 声称支持 cardinal 差分；严格单调变换不能修复固定 archive 下当前选择器的排序偏好。
