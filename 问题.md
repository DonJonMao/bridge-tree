## 结论

**现在可以开始跑小规模实验和单次配置试运行，但我不建议立刻启动全量 PersonaMem-32K 配置搜索或三随机种子主表。**

当前核心检索器已经具备可运行条件：

* 所有模块都能通过 CLI 或启动脚本参数控制；
* 默认配置已经收敛为我们讨论的 Core 版本：固定聚类、深度 2、瓶颈可达性、\(\rho\)-logdet、预算停止；
* BridgeTree、RF-Mem、cluster-PRF 等动态方法已经共用成本追踪接口；
* 轻量诊断不会额外调用 ANN；
* 测试覆盖了真实父边、路径单调性、桥接发现、证书上界、运行时参数覆盖等核心性质。([GitHub][1])

但当前还有 **3 个需要在全量运行前处理的问题**，其中第一个是配置问题，后两个是代码与实验公平性问题。

另外，仓库中的 `train.sh` 实际执行的是**无权重更新的超参数配置搜索**，不是模型训练，这与方案的“无蒸馏、无任务级微调”边界一致。([GitHub][2])

---

# 一、当前默认 `train.yaml` 不能完成有效选参

这是现在最直接的问题。

当前配置是：

```yaml
objective_metric: auto
bridge_gold_path: null
validation_generate: false
final_generate: false
```

在这种情况下，代码会将 tuning objective 解析为 `None`。最终只会输出各配置的成本 Pareto 报告，不会：

* 选择最佳配置；
* 生成 `best_config.json`；
* 运行 test split；
* 输出最终答案准确率比较。

这并不是 bug，而是你为了避免使用内部 log-det 指标选自己方法而设计的保护机制。([GitHub][3])

因此，直接运行：

```bash
./scripts/train.sh
```

可以验证代码流程，但不能叫作一次有效选参实验。

## PersonaMem 上建议使用答案准确率作为验证目标

PersonaMem-v1 没有逐条 gold memory ID，因此目前最现实的外部目标是：

```yaml
objective_metric: outcome.answer_accuracy
validation_generate: true
final_generate: true
```

代码已经支持这种模式，并会在验证集上选配置，然后用相同生成器和提示对所有主表方法进行最终测试。([GitHub][4])

---

# 二、全量运行前必须修复：失败样本目前可能污染选参

当前 `TrainingEvaluator.evaluate_set()` 会捕获每个查询上的异常，将其写入 `failures.jsonl`，然后继续运行。但汇总指标只基于成功完成的样本：

```python
records = []
successful_question_ids = []

for example in examples:
    try:
        records.append(...)
        successful_question_ids.append(...)
    except Exception:
        write_failure(...)
```

随后 answer accuracy 和逐问题指标都只来自 `records`。([GitHub][4])

这会产生一个危险情况：

* 配置 A 完成 100 个问题，准确率 55%；
* 配置 B 在 30 个困难问题上异常，只完成 70 个，准确率 60%；
* 当前逻辑有可能认为 B 更好。

虽然 `_is_better()` 会尽量基于共同成功的问题做 paired bootstrap，但失败样本仍然没有被惩罚，配置之间的有效评价集合也可能不同。([GitHub][4])

## 建议修复

在 `evaluate_set()` 中增加：

```python
attempted_queries = len(examples)
successful_queries = len(records)
failed_queries = attempted_queries - successful_queries
failure_rate = failed_queries / attempted_queries
```

写入 summary：

```python
summary.update(
    {
        "attempted_queries": attempted_queries,
        "successful_queries": successful_queries,
        "failed_queries": failed_queries,
        "failure_rate": failure_rate,
    }
)
```

对 tuning 阶段建议直接采用严格模式：

```python
if failed_queries > 0:
    raise RuntimeError(
        f"{method} failed on {failed_queries}/{attempted_queries} evaluation queries"
    )
```

远程接口本身已经有三次重试，因此正式选参阶段要求零失败是合理的。

至少也要把失败的答案准确率计为 0，并确保所有配置在完全相同的 question ID 集合上比较。否则全量跑完后的最佳配置可能不可信。

---

# 三、基线的 `search_budget` 目前没有被完全统一执行

`SearchBudget` 定义了三个约束：

$$
B_{\text{node}},\qquad B_{\text{ANN}},\qquad B_{\text{candidate}}.
$$

但是目前 `CostTracker.can_search_core()` 只检查：

* 剩余 ANN 调用数；
* 剩余 candidate exposure。

它没有检查：

$$
\text{max\_unique\_nodes}.
$$

而 `search_core()` 也没有根据剩余唯一节点数截断 `top_k`。([GitHub][5])

BridgeTree 自己在 `_expand_one()` 中通过：

```python
capacity = min(
    branch_width,
    budget.max_unique_nodes - len(nodes),
)
```

控制了节点数，因此 BridgeTree 基本安全。([GitHub][6])

但 RF-Mem 和 cluster-PRF 依赖各自的局部状态控制。例如 RF-Mem 的 `seen` 在一个 beam round 结束后才更新，多分支在同一轮中可能使 `CostTracker` 实际访问的唯一节点超过 `max_unique_nodes`。当前测试只断言了 ANN 调用数和 candidate exposure，没有断言：

```python
cost.unique_visited_nodes <= budget.max_unique_nodes
```

([GitHub][7])

## 建议修复

在 `CostTracker` 中加入：

```python
@property
def remaining_unique_nodes(self) -> int:
    return max(0, self.budget.max_unique_nodes - len(self._visited))
```

修改：

```python
def can_search_core(self) -> bool:
    return (
        self.remaining_unique_nodes > 0
        and (self.remaining_ann_calls is None or self.remaining_ann_calls > 0)
        and (
            self.remaining_candidate_exposure is None
            or self.remaining_candidate_exposure > 0
        )
    )
```

并在 `search_core()` 中增加：

```python
top_k = min(top_k, self.remaining_unique_nodes)
```

测试增加：

```python
assert all(
    cost.unique_visited_nodes <= budget.max_unique_nodes
    for cost in costs
)
```

这个问题不影响你先跑 BridgeTree 单方法，但在与 RF-Mem 做 matched-budget 主表之前必须修复。

---

# 四、RF-Mem 快路径目前多做了一次无意义检索

当前 RF-Mem 首先检索 10 条作为路由 probe：

```python
probe = tracker.search_core(...)
```

当路由进入 fast/Familiarity 时，又对同一个原始查询执行了一次 Top-\(k\)：

```python
hits = tracker.search_core(index, query_vector, top_k)
```

对于默认的：

$$
k=5,\qquad |\text{probe}|=10,
$$

第二次搜索不会带来新结果，只会重复前一次 Top-\(k\)，并额外增加一次 ANN 调用和 candidate exposure。([GitHub][7])

应该直接复用：

```python
selected = [
    memory_id
    for memory_id, score in probe[:top_k]
    if score >= 0.3
]
```

只有当：

```python
len(probe) < top_k
```

时才需要补充搜索。

这个问题不会让 BridgeTree 的准确率虚高，但会让 RF-Mem 的效率看起来不必要地差，因此在论文的效率比较之前应修复。

---

# 五、当前 tuning 中的“训练循环”实际上没有训练作用

每个 trial 当前会执行：

1. 遍历 train split；
2. 只运行 BridgeTree retrieval；
3. 每隔 50 个 train query，重新运行相同 validation probe；
4. 最后再运行完整 validation；
5. 根据 validation 选配置。

但每个 trial 内配置是固定的，没有参数更新。因此：

```text
处理第 1–50 个训练问题
→ 验证一次

处理第 51–100 个训练问题
→ 再验证一次
```

不会产生任何学习进展。重复验证最多只能反映缓存预热或接口波动，不是训练曲线。([GitHub][4])

它不是正确性 blocker，但会浪费大量时间。

## 第一版建议

把 tuning 简化成：

```text
每个候选配置
    → 在 validation split 上运行一次
    → 按外部答案准确率选最佳配置

最佳配置
    → 在 test split 上运行一次主表方法
```

train split 可以保留用于：

* 开发诊断；
* 参数范围预估；
* 构建 Bridge 子集；
* 单独运行模块分析。

但不必在每个 trial 中完整遍历。

短期内不改代码也可以，只需在 pilot 配置中设置：

```yaml
schedule:
  periodic_eval_every: 1000000
  periodic_eval_queries: 3
  max_train_queries: 3
```

由于代码仍会在最后一步做一次 probe，这会把无效的 train pass 压到最低。

---

# 六、当前模块消融定义中有重复项

默认 BridgeTree 配置已经是：

```text
fixed clustering
best_first
rho feature
rho_logdet
budget stop
depth = 2
```

([GitHub][8])

但 `ABLATION_OPTIONS` 中：

```python
ablation_rho_dpp = rho + rho_logdet + budget
```

因此在默认配置下：

$$
\text{ablation\_rho\_dpp}
=
\text{bridgetree}.
$$

同时：

```python
ablation_topk
```

和：

```python
ablation_direct_path
```

在默认 `feature_mode=rho` 下也基本相同；`ablation_fixed_depth` 则把深度从 2 改为 3，严格来说是“增加模块”，不是去掉模块。([GitHub][9])

所以当前 `module_effects.jsonl` 中可能出现：

* 完整方法减自己；
* 两个重复消融；
* 名称和实际变化不一致。

## 当前阶段的处理方法

配置搜索时只保留：

```yaml
diagnostic_methods:
  - bridgetree
```

不要在每一个超参数 trial 中跑全部消融。

配置选完后，使用已经写好的参数矩阵脚本：

```bash
GENERATE=true ./scripts/ablation.sh
```

单独比较：

* Core；
* +Path；
* +Certificate；
* Full-current；
* no-cluster；
* BFS；
* depth 1/2/3；
* Top-\(k\)；
* MMR。

这个脚本的参数关系比 `ABLATION_OPTIONS` 更清楚。([GitHub][10])

---

# 七、当前预算设置会让 Best-first 与 BFS 很可能完全一样

这是一个容易忽略但很关键的问题。

默认设置为：

$$
b_0=12,\quad C=4,\quad L=8,\quad D_{\max}=2,\quad B_s=64.
$$

深度 2 时，最多发现的节点约为：

$$
12+4\times8=44.
$$

但预算是：

$$
B_s=64.
$$

因此所有一级聚类分支都能够被完全扩展。默认又采用 `budget` 停止，代码会先把所有可扩展分支展开，再在最终候选池中选取记忆。([GitHub][8])

于是：

$$
\boxed{
\text{Best-first 与 BFS 只改变扩展顺序，不改变最终候选集合。}
}
$$

当前 `configs/train.yaml` 甚至只搜索：

```yaml
search_budget: [64]
```

因此用现有设置几乎验证不出 Best-first 的价值。([GitHub][3])

## 建议搜索预算

第一轮建议使用：

```yaml
search_space:
  initial_width: [8, 12]
  branch_width: [4, 8]
  search_budget: [20, 28, 36, 44]
```

其中：

* 20：明显受限，只能扩展少量高优先级分支；
* 28：中等预算；
* 36：接近完整；
* 44：完整深度 2 上界附近。

这样才能看到：

$$
\text{Best-first 是否在相同节点预算下优于 BFS}.
$$

若只做快速 pilot，可以先用：

```yaml
search_budget: [20, 36]
```

---

# 八、目前哪些实验可以立即开始

| 实验                       |      当前能否开始 | 说明                    |
| ------------------------ | ----------: | --------------------- |
| 单元测试和 Ruff               |      **可以** | 必须先本地实际执行             |
| synthetic bridge smoke   |      **可以** | 不依赖模型服务               |
| 5–10 条 retrieval-only 试跑 |      **可以** | 验证 embedding、缓存和结果输出  |
| 3–5 条 end-to-end 生成      |      **可以** | 验证 generator 与答案解析    |
| 单配置 tuning pilot         | **修改配置后可以** | 必须开启外部答案指标            |
| 全量 32K tuning            |      **暂缓** | 先修失败处理和无效 train loop  |
| 多基线主表                    |      **暂缓** | 先修唯一节点预算和 RF-Mem 重复调用 |
| 三随机种子正式实验                |  **不要现在启动** | 成本太高，且当前消融定义仍有重复      |

这也符合我们之前的判断：核心代码本身并不是最难的部分，真正容易让 NLP 顶会审稿人质疑的是基线预算是否公平、目标记忆是否独立定义，以及增益是否真的来自桥接而不是更多候选。

---

# 九、建议立即执行的检查顺序

我无法在当前容器中克隆 GitHub 仓库，因此这次是对今天公开 `main` 源码的静态审查，不能替你声称 `pytest` 已经实际通过。服务器上先执行：

```bash
git pull
git rev-parse HEAD

python3 -m venv .venv
.venv/bin/pip install -e '.[test]'

.venv/bin/pytest -q
.venv/bin/ruff check src tests
./scripts/smoke_synthetic.sh
```

项目已经在 `pyproject.toml` 中声明了 pytest 和 Ruff 测试依赖。([GitHub][11])

然后验证数据和模型端点：

```bash
.venv/bin/bridgetree download-personamem --split 32k
.venv/bin/bridgetree prepare-personamem --split 32k

# Embedding + BridgeTree，不调用生成器
DIAGNOSTIC_LEVEL=light \
GENERATE=false \
./scripts/run_personamem.sh --limit 5

# 生成器和答案解析
METHOD=dense \
GENERATE=true \
./scripts/run_personamem.sh --limit 3

METHOD=bridgetree \
GENERATE=true \
./scripts/run_personamem.sh --limit 3
```

每一步检查：

```text
failures.jsonl 必须为空
summary.json 中 failed_queries = 0
parse_failure_rate = 0
final_context_count 接近 context_size
selected_memory_ids 非空
ann_calls_core 与 candidates_returned 非零
```

---

# 十、用于第一次有效 tuning pilot 的配置

建议新建 `configs/tune_pilot.yaml`，不要直接覆盖正式配置：

```yaml
seed: 42

split:
  train_ratio: 0.70
  validation_ratio: 0.15
  test_ratio: 0.15

schedule:
  periodic_eval_every: 1000000
  periodic_eval_queries: 3
  max_train_queries: 3
  max_validation_queries: 20
  max_test_queries: 20

search_space:
  initial_width: [8, 12]
  branch_width: [4, 8]
  search_budget: [20, 36]

diagnostic_methods:
  - bridgetree

main_table_methods:
  - dense
  - rfmem_recollection
  - rfmem
  - cluster_prf
  - bridgetree

objective_metric: outcome.answer_accuracy
objective_mode: max
bridge_gold_path: null
validation_generate: true
final_generate: true
keep_example_metrics: true
output_dir: outputs/tuning-pilot
```

运行：

```bash
./scripts/train.sh \
  --tuning-config configs/tune_pilot.yaml \
  --cluster-mode fixed \
  --cluster-count 4 \
  --feature-mode rho \
  --selection-mode rho_logdet \
  --stop-mode budget \
  --max-depth 2 \
  --diagnostic-level light \
  --index-backend exact
```

这将运行：

$$
2\times2\times2=8
$$

个候选配置。验证集 20 个问题，规模足够检查配置选择链路，又不会立即消耗完整实验成本。

成功标准是：

```text
final_summary.json:
  selection_status = selected_on_external_validation_outcome
  best_trial != null
  objective_metric = outcome.answer_accuracy

best_config.json 存在
failures.jsonl 为空
验证与测试 parse_failure_rate 接近 0
所有方法测试查询数量一致
```

---

# 最终裁决

**核心代码已经达到“可以开始 pilot”的状态，尤其是运行时参数化、默认 Core 配置、成本分层和测试覆盖都比上一版完善很多。**

但：

$$
\boxed{
\text{现在不要直接跑默认 }./scripts/train.sh
}
$$

因为它没有外部目标，不会选出最佳配置。

更准确的上线顺序是：

$$
\boxed{
\text{单测}
\rightarrow
\text{synthetic smoke}
\rightarrow
\text{5 条检索试跑}
\rightarrow
\text{3 条生成试跑}
\rightarrow
\text{20 条 tuning pilot}
}
$$

在 full 32K 之前，至少完成：

1. tuning 失败样本严格处理；
2. 所有基线统一执行唯一节点预算；
3. RF-Mem Familiarity 复用 probe；
4. tuning 中只跑 BridgeTree，消融单独执行；
5. 使用能真正限制扩展的预算范围。

修完这五项后，就可以比较放心地启动正式配置搜索。

[1]: https://raw.githubusercontent.com/DonJonMao/bridge-tree/main/src/bridgetree/cli.py "https://raw.githubusercontent.com/DonJonMao/bridge-tree/main/src/bridgetree/cli.py"
[2]: https://raw.githubusercontent.com/DonJonMao/bridge-tree/main/scripts/train.sh "https://raw.githubusercontent.com/DonJonMao/bridge-tree/main/scripts/train.sh"
[3]: https://raw.githubusercontent.com/DonJonMao/bridge-tree/main/configs/train.yaml "https://raw.githubusercontent.com/DonJonMao/bridge-tree/main/configs/train.yaml"
[4]: https://raw.githubusercontent.com/DonJonMao/bridge-tree/main/src/bridgetree/training.py "https://raw.githubusercontent.com/DonJonMao/bridge-tree/main/src/bridgetree/training.py"
[5]: https://raw.githubusercontent.com/DonJonMao/bridge-tree/main/src/bridgetree/budget.py "https://raw.githubusercontent.com/DonJonMao/bridge-tree/main/src/bridgetree/budget.py"
[6]: https://raw.githubusercontent.com/DonJonMao/bridge-tree/main/src/bridgetree/retriever.py "https://raw.githubusercontent.com/DonJonMao/bridge-tree/main/src/bridgetree/retriever.py"
[7]: https://raw.githubusercontent.com/DonJonMao/bridge-tree/main/src/bridgetree/baselines.py "https://raw.githubusercontent.com/DonJonMao/bridge-tree/main/src/bridgetree/baselines.py"
[8]: https://raw.githubusercontent.com/DonJonMao/bridge-tree/main/configs/default.yaml "https://raw.githubusercontent.com/DonJonMao/bridge-tree/main/configs/default.yaml"
[9]: https://raw.githubusercontent.com/DonJonMao/bridge-tree/main/src/bridgetree/experiment.py "https://raw.githubusercontent.com/DonJonMao/bridge-tree/main/src/bridgetree/experiment.py"
[10]: https://raw.githubusercontent.com/DonJonMao/bridge-tree/main/scripts/ablation.sh "https://raw.githubusercontent.com/DonJonMao/bridge-tree/main/scripts/ablation.sh"
[11]: https://raw.githubusercontent.com/DonJonMao/bridge-tree/main/pyproject.toml "https://raw.githubusercontent.com/DonJonMao/bridge-tree/main/pyproject.toml"
